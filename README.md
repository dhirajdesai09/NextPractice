# Simple Next.js Full-Stack App

A minimal Next.js project with a client-side page and a backend API endpoint.

## Run it

```bash
npm install
npm run dev
```

Open http://localhost:3000. The button calls `GET /api/hello`.
